// AUTO-GENERATED FILE - DO NOT EDIT
// Generated by scripts/build-blog-content.ts

export interface BlogPost {
  slug: string;
  title: string;
  description: string;
  date: string;
  modified?: string;
  tags: string[];
  coverUrl?: string;
  coverWidth?: number;
  coverHeight?: number;
  people: string[];
  type: string;
  draft: boolean;
  html: string;
  wordCount: number;
  readingTime: number;
  assets: string[];
}

export const posts: BlogPost[] = [
  {
    "slug": "building my own os from scratch:  p3&4&5",
    "title": "Writing an x86 OS: Memory, Graphics, Driver Abstractions, and FAT32",
    "description": "Continuing the bare-metal x86 OS: heap allocator over the Multiboot memory map, operator new wired to a global MemoryManager, VGA Mode 13h entered via direct register programming, a driver abstraction layer with BAR parsing, ATA PIO disk I/O, MBR parsing, and a FAT32 reader.",
    "date": "2026-03-02",
    "modified": "2026-03-02",
    "tags": [
      "OS",
      "x86",
      "systems",
      "memory-management",
      "filesystems",
      "graphics",
      "c++"
    ],
    "people": [
      "author"
    ],
    "type": "article",
    "draft": false,
    "html": "<h1>Writing an x86 OS: Memory, Graphics, Driver Abstractions, and FAT32</h1>\n<p>With protected mode, interrupts, and basic hardware drivers in place, the second half of the project deals with memory management, a proper driver model, getting into graphics mode, and reading files off a disk. These phases are where the architectural decisions start to compound,   a poorly designed allocator or driver interface creates friction through every layer that follows.</p>\n<hr>\n<h2>Memory Management: The Multiboot Map and the Heap</h2>\n<p>GRUB passes a Multiboot info structure to <code>kernelMain</code> with a memory map describing which physical regions are usable. The map is a flat array of variable-length entries,   each has a base address, a length, and a type (1 = usable RAM). The iteration has a non-obvious stride: the entry&#39;s <code>size</code> field counts bytes excluding itself, so the next entry is at <code>(uint8_t*)entry + entry-&gt;size + 4</code>.</p>\n<p>Two regions need to be excluded from the heap:</p>\n<ul>\n<li>Anything below <code>0x00200000</code>,   the kernel itself, the GRUB stack, the multiboot structures, and the ISA hole all live here</li>\n<li>Any region above <code>0xFFFFFFFF</code>,   64-bit base addresses are possible in the map on PAE systems, but we&#39;re in 32-bit mode</li>\n</ul>\n<p>Of the remaining usable regions, the largest is selected as the heap.</p>\n<p>The allocator is an intrusive linked list. Each allocation is preceded by a <code>MemoryChunk</code> header in-band with the memory it describes:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-keyword\">struct</span> <span class=\"hljs-title class_\">MemoryChunk</span> {\n, MemoryChunk* prev;\n, MemoryChunk* next;\n, <span class=\"hljs-type\">bool</span>, ,  allocated;\n, <span class=\"hljs-type\">uint32_t</span>,  size;\n} __attribute__((packed));\n</code></pre><p><code>malloc</code> does a first-fit walk. When a free chunk is large enough to hold the requested size plus a <code>MemoryChunk</code> header with leftover space, it splits:</p>\n<pre><code class=\"hljs language-cpp\">MemoryChunk* tail = (MemoryChunk*)((<span class=\"hljs-type\">uint8_t</span>*)chunk + <span class=\"hljs-built_in\">sizeof</span>(MemoryChunk) + size);\ntail-&gt;size = chunk-&gt;size - size - <span class=\"hljs-built_in\">sizeof</span>(MemoryChunk);\ntail-&gt;prev = chunk;\ntail-&gt;next = chunk-&gt;next;\ntail-&gt;allocated = <span class=\"hljs-literal\">false</span>;\n<span class=\"hljs-keyword\">if</span> (chunk-&gt;next) chunk-&gt;next-&gt;prev = tail;\nchunk-&gt;next = tail;\nchunk-&gt;size = size;\n</code></pre><p><code>free</code> coalesces in both directions to prevent fragmentation: merge with <code>next</code> if it&#39;s free, then check if <code>prev</code> is free and merge the combined block back into it.</p>\n<h3>Wiring <code>operator new</code></h3>\n<p>With <code>-nostdlib</code>, <code>operator new</code> is undefined. Any use of <code>new</code> in driver code or GUI objects causes a linker error. The fix is a small translation unit that delegates to a global allocator pointer:</p>\n<pre><code class=\"hljs language-cpp\">MemoryManager* active_memory_manager = <span class=\"hljs-literal\">nullptr</span>;\n\n<span class=\"hljs-function\"><span class=\"hljs-type\">void</span>* <span class=\"hljs-keyword\">operator</span> <span class=\"hljs-title\">new</span><span class=\"hljs-params\">(<span class=\"hljs-type\">uint32_t</span> size)</span>   </span>{ <span class=\"hljs-keyword\">return</span> active_memory_manager-&gt;<span class=\"hljs-built_in\">malloc</span>(size); }\n<span class=\"hljs-function\"><span class=\"hljs-type\">void</span>  <span class=\"hljs-keyword\">operator</span> <span class=\"hljs-title\">delete</span><span class=\"hljs-params\">(<span class=\"hljs-type\">void</span>* ptr)</span>, </span>{ active_memory_manager-&gt;<span class=\"hljs-built_in\">free</span>(ptr); }\n</code></pre><p><code>active_memory_manager</code> must be set before anything else initialises. In <code>kernelMain</code>, the first two lines after constructing the <code>MemoryManager</code> are:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-function\">MemoryManager <span class=\"hljs-title\">mm</span><span class=\"hljs-params\">(heap_start, heap_size)</span></span>;\nactive_memory_manager = &amp;mm;\n</code></pre><p>The ordering matters,   <code>InterruptManager</code>, <code>KeyboardDriver</code>, and <code>DriverManager</code> all potentially call <code>operator new</code> during construction.</p>\n<hr>\n<h2>VGA: Replacing the Naked Global</h2>\n<p>The first phase&#39;s <code>keyboard.cpp</code>, <code>pci.cpp</code>, and <code>kernel.cpp</code> all shared a naked <code>extern int vga_cursor</code> and wrote directly to <code>(uint16_t*)0xB8000</code>. That&#39;s tolerable for three files; it doesn&#39;t survive Phase 4 where new drivers and GUI code all need to print.</p>\n<p>The replacement is a static-method VGA class,   no instantiation, no state outside of a cursor position and a colour byte:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title class_\">VGA</span> {\n<span class=\"hljs-keyword\">public</span>:\n, <span class=\"hljs-function\"><span class=\"hljs-type\">static</span> <span class=\"hljs-type\">void</span> <span class=\"hljs-title\">PutChar</span><span class=\"hljs-params\">(<span class=\"hljs-type\">char</span> c)</span></span>;\n, <span class=\"hljs-function\"><span class=\"hljs-type\">static</span> <span class=\"hljs-type\">void</span> <span class=\"hljs-title\">Print</span><span class=\"hljs-params\">(<span class=\"hljs-type\">const</span> <span class=\"hljs-type\">char</span>* str)</span></span>;\n, <span class=\"hljs-function\"><span class=\"hljs-type\">static</span> <span class=\"hljs-type\">void</span> <span class=\"hljs-title\">PrintHex</span><span class=\"hljs-params\">(<span class=\"hljs-type\">uint8_t</span> val)</span></span>;\n, <span class=\"hljs-comment\">// ...</span>\n<span class=\"hljs-keyword\">private</span>:\n, <span class=\"hljs-type\">static</span> <span class=\"hljs-type\">uint16_t</span>* <span class=\"hljs-type\">const</span> buffer;  <span class=\"hljs-comment\">// 0xB8000</span>\n, <span class=\"hljs-type\">static</span> <span class=\"hljs-type\">uint16_t</span> cursor;\n, <span class=\"hljs-type\">static</span> <span class=\"hljs-type\">uint8_t</span>  color;\n};\n</code></pre><p><code>PutChar</code> handles <code>\\b</code>, <code>\\n</code>, and <code>\\t</code> explicitly. Everything else is <code>(color &lt;&lt; 8) | c</code> written at the cursor position. This replaces every direct VGA write in the codebase with a single call site.</p>\n<h3>Mode 13h: Direct Register Programming</h3>\n<p>Entering 320×200 256-colour mode without BIOS means programming four VGA register groups directly through port I/O. The groups are:</p>\n<ul>\n<li><strong>Sequencer</strong> (<code>0x3C4</code>/<code>0x3C5</code>),   controls memory plane access and clock</li>\n<li><strong>CRTC</strong> (<code>0x3D4</code>/<code>0x3D5</code>),   timing: horizontal/vertical totals, blanking, retrace</li>\n<li><strong>Graphics Controller</strong> (<code>0x3CE</code>/<code>0x3CF</code>),   read/write mode, memory map select</li>\n<li><strong>Attribute Controller</strong> (<code>0x3C0</code>),   palette, overscan, colour plane enables</li>\n</ul>\n<p>Each is an indexed register pair: write the index byte to the index port, then the value byte to the data port.</p>\n<p>The Attribute Controller is the awkward one. It shares a single port (<code>0x3C0</code>) for both index and data,   you write index and data alternately, and an internal flip-flop tracks which is expected next. Before writing anything, you must reset that flip-flop by reading port <code>0x3DA</code>:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-function\">Port8Bit <span class=\"hljs-title\">flipflop_reset</span><span class=\"hljs-params\">(<span class=\"hljs-number\">0x3DA</span>)</span></span>;\nflipflop_reset.<span class=\"hljs-built_in\">Read</span>();  <span class=\"hljs-comment\">// reset AC flip-flop</span>\n<span class=\"hljs-keyword\">for</span> (<span class=\"hljs-type\">int</span> i = <span class=\"hljs-number\">0</span>; i &lt; <span class=\"hljs-number\">21</span>; i++) {\n, ac.<span class=\"hljs-built_in\">Write</span>(i);, , <span class=\"hljs-comment\">// index</span>\n, ac.<span class=\"hljs-built_in\">Write</span>(ac_values[i]);  <span class=\"hljs-comment\">// data</span>\n}\nac.<span class=\"hljs-built_in\">Write</span>(<span class=\"hljs-number\">0x20</span>);  <span class=\"hljs-comment\">// re-enable display</span>\n</code></pre><p>After the register sequence, the framebuffer lives at <code>0xA0000</code>. Pixel (x, y) is at offset <code>y * 320 + x</code>. Palette entries 0–15 match the standard CGA/EGA colours, which is enough for a basic GUI without reprogramming the DAC.</p>\n<hr>\n<h2>Driver Abstractions and BAR Parsing</h2>\n<p>Up to Phase 3, PCI enumeration printed device info but never instantiated any drivers. <code>SelectDrivers(InterruptManager*)</code> received a pointer it didn&#39;t use. The fix is a formal driver model.</p>\n<p><code>Driver</code> is a base class with virtual <code>Activate</code>, <code>Deactivate</code>, and <code>Reset</code>. <code>DriverManager</code> holds an array of <code>Driver*</code> and exposes <code>AddDriver</code> / <code>ActivateAll</code>. <code>SelectDrivers</code> becomes:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-function\"><span class=\"hljs-type\">void</span> <span class=\"hljs-title\">PCI::SelectDrivers</span><span class=\"hljs-params\">(DriverManager* dm, InterruptManager* interrupts)</span> </span>{\n, <span class=\"hljs-comment\">// enumerate...</span>\n, Driver* drv = <span class=\"hljs-built_in\">GetDriver</span>(desc, interrupts);\n, <span class=\"hljs-keyword\">if</span> (drv) dm-&gt;<span class=\"hljs-built_in\">AddDriver</span>(drv);\n}\n</code></pre><p><code>GetDriver</code> matches on <code>class_id</code>/<code>subclass_id</code> and returns a concrete driver instance. When <code>ActivateAll</code> is called later, each driver&#39;s <code>Activate</code> method fires,   the ATA driver uses this to run <code>IDENTIFY</code> and confirm whether a drive is present.</p>\n<h3>BAR Sizing</h3>\n<p><code>PCIDeviceDescriptor</code> had <code>port_base</code> and <code>mem_base</code> both hardcoded to 0 because <code>GetDeviceDescriptor</code> never read the BAR offsets (<code>0x10</code>–<code>0x24</code>). The BAR sizing algorithm fills them:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-built_in\">Write</span>(bus, device, func, offset, <span class=\"hljs-number\">0xFFFFFFFF</span>);\n<span class=\"hljs-type\">uint32_t</span> sized = <span class=\"hljs-built_in\">Read</span>(bus, device, func, offset);\n<span class=\"hljs-built_in\">Write</span>(bus, device, func, offset, original);\n\n<span class=\"hljs-comment\">// I/O BAR: bit 0 set</span>\n<span class=\"hljs-keyword\">if</span> (original &amp; <span class=\"hljs-number\">0x1</span>) {\n, port_base = original &amp; <span class=\"hljs-number\">0xFFFFFFFC</span>;\n, bar_size  = ~(sized &amp; <span class=\"hljs-number\">0xFFFFFFFC</span>) + <span class=\"hljs-number\">1</span>;\n} <span class=\"hljs-keyword\">else</span> {\n, mem_base = original &amp; <span class=\"hljs-number\">0xFFFFFFF0</span>;\n, bar_size = ~(sized &amp; <span class=\"hljs-number\">0xFFFFFFF0</span>) + <span class=\"hljs-number\">1</span>;\n}\n</code></pre><p>Writing all-ones forces the BAR to report its size mask on the next read. The invert-and-add-one converts the mask to a byte count. Restoring the original value is critical,   leaving <code>0xFFFFFFFF</code> in a BAR will corrupt the device&#39;s address decoding.</p>\n<hr>\n<h2>GUI Layer</h2>\n<p>The GUI is a three-level hierarchy: <code>Widget</code> (bounding box, virtual event handlers) → <code>Window</code> (title bar, child widget list) → <code>Desktop</code> (background, window list, mouse state).</p>\n<p><code>Desktop::OnMouseMove</code> erases the old cursor pixel by drawing the desktop background colour, updates position, then draws a new white pixel. <code>OnMouseDown</code> iterates windows in reverse insertion order (topmost first) and delivers the event to the first one whose bounding box contains the click point. Keyboard events go to the last window in the list,   a stand-in for proper focus management.</p>\n<p>Wiring the keyboard and mouse drivers to <code>Desktop</code> without adding parameters to existing constructors: a file-scope <code>Desktop*</code> in <code>gui.cpp</code> with <code>SetActiveDesktop</code> / <code>GetActiveDesktop</code>. <code>keyboard.cpp</code> and <code>mouse.cpp</code> call <code>GetActiveDesktop()</code> with a null check at the top of their interrupt handlers. This keeps the driver constructors unchanged and the coupling explicit.</p>\n<hr>\n<h2>ATA PIO</h2>\n<p>The primary IDE controller sits at the well-known addresses: data at <code>0x1F0</code> (16-bit), status/command at <code>0x1F7</code>, drive/head select at <code>0x1F6</code>, LBA bytes at <code>0x1F3</code>–<code>0x1F5</code>.</p>\n<p>LBA28 read:</p>\n<pre><code class=\"hljs language-cpp\">device_port.<span class=\"hljs-built_in\">Write</span>(<span class=\"hljs-number\">0xE0</span> | ((lba &gt;&gt; <span class=\"hljs-number\">24</span>) &amp; <span class=\"hljs-number\">0x0F</span>));  <span class=\"hljs-comment\">// drive 0, LBA mode</span>\nsector_count_port.<span class=\"hljs-built_in\">Write</span>(count);\nlba_low_port.<span class=\"hljs-built_in\">Write</span>(lba &amp; <span class=\"hljs-number\">0xFF</span>);\nlba_mid_port.<span class=\"hljs-built_in\">Write</span>((lba &gt;&gt; <span class=\"hljs-number\">8</span>) &amp; <span class=\"hljs-number\">0xFF</span>);\nlba_hi_port.<span class=\"hljs-built_in\">Write</span>((lba &gt;&gt; <span class=\"hljs-number\">16</span>) &amp; <span class=\"hljs-number\">0xFF</span>);\ncommand_port.<span class=\"hljs-built_in\">Write</span>(<span class=\"hljs-number\">0x20</span>);  <span class=\"hljs-comment\">// READ SECTORS</span>\n\n<span class=\"hljs-keyword\">for</span> (<span class=\"hljs-type\">int</span> s = <span class=\"hljs-number\">0</span>; s &lt; count; s++) {\n, <span class=\"hljs-built_in\">WaitBSY</span>();\n, <span class=\"hljs-built_in\">WaitDRQ</span>();\n, <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-type\">int</span> i = <span class=\"hljs-number\">0</span>; i &lt; <span class=\"hljs-number\">256</span>; i++)\n, , ((<span class=\"hljs-type\">uint16_t</span>*)buffer)[s * <span class=\"hljs-number\">256</span> + i] = data_port.<span class=\"hljs-built_in\">Read</span>();\n}\n</code></pre><p><code>WaitBSY</code> spins while bit 7 of the status register is set. <code>WaitDRQ</code> spins until bit 3 is set. This is polling,   interrupts on IRQ14 are registered through the existing handler table, but not used for synchronisation here. The interrupt handler just returns <code>esp</code>.</p>\n<p>The <code>IDENTIFY</code> command (<code>0xEC</code>) returns 256 words of drive metadata. We only use it to verify a drive is present,   if the status register reads <code>0x00</code> after the command, there&#39;s no drive.</p>\n<hr>\n<h2>MBR</h2>\n<p>The MBR is sector 0. The layout: 446 bytes of bootstrap code, four 16-byte partition entries, two-byte signature <code>0xAA55</code>. The partition entry gives you <code>lba_start</code> and <code>sector_count</code> for each partition, plus a type byte to identify the filesystem (<code>0x0B</code>/<code>0x0C</code> for FAT32, <code>0x83</code> for ext2/3/4).</p>\n<p>The parser reads sector 0 via the ATA driver, copies it into a packed struct, and checks the signature. No stdlib <code>memcpy</code> available,   a local two-byte loop handles the copy.</p>\n<hr>\n<h2>FAT32</h2>\n<p>The BPB at the start of the partition&#39;s first sector gives everything needed to navigate the volume:</p>\n<pre><code>fat_lba  = partition_lba + bpb.reserved_sectors\ndata_lba = fat_lba + bpb.fat_count * bpb.fat_size_32\n</code></pre><p>Cluster-to-LBA:</p>\n<pre><code>lba = data_lba + (cluster - 2) * bpb.sectors_per_cluster\n</code></pre><p>The subtraction of 2 is because clusters 0 and 1 are reserved.</p>\n<p>FAT chain traversal reads one FAT sector per step:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-type\">uint32_t</span> fat_offset  = cluster * <span class=\"hljs-number\">4</span>;\n<span class=\"hljs-type\">uint32_t</span> fat_sector  = fat_lba + (fat_offset / <span class=\"hljs-number\">512</span>);\n<span class=\"hljs-type\">uint32_t</span> entry_off   = fat_offset % <span class=\"hljs-number\">512</span>;\n<span class=\"hljs-built_in\">ReadSector</span>(fat_sector, buf);\n<span class=\"hljs-type\">uint32_t</span> next = *(<span class=\"hljs-type\">uint32_t</span>*)(buf + entry_off) &amp; <span class=\"hljs-number\">0x0FFFFFFF</span>;\n<span class=\"hljs-comment\">// next &gt;= 0x0FFFFFF8 means end of chain</span>\n</code></pre><p>Directory entries are 32-byte structs. The 8.3 filename is stored as an 11-byte space-padded uppercase field,   <code>&quot;README  TXT&quot;</code> not <code>&quot;README.TXT&quot;</code>. Entries with <code>name[0] == 0x00</code> mark the end of the directory. Entries with <code>name[0] == 0xE5</code> are deleted. <code>attribute == 0x0F</code> marks a long filename (LFN) entry,   skipped entirely here.</p>\n<p>Path traversal walks one component at a time, calling <code>FindEntry</code> at each directory cluster, following the cluster chain for each directory until the name matches or the chain ends.</p>\n<hr>\n<h2>Testing</h2>\n<p>Disk testing uses QEMU&#39;s <code>-hda</code> flag with a raw disk image:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">dd</span> <span class=\"hljs-keyword\">if</span>=/dev/zero of=disk.img bs=512 count=131072\nmkfs.fat -F 32 disk.img\n<span class=\"hljs-comment\"># mount and populate with test files</span>\nqemu-system-i386 -cdrom mykernel.iso -hda disk.img\n</code></pre><p>The <code>-d int</code> flag on QEMU&#39;s command line logs all interrupt vectors to stderr. It&#39;s the fastest way to identify what&#39;s triple-faulting,   the last few entries before the CPU resets show exactly which vector fired and whether it was handled.</p>\n<p>A recurring issue during this phase was ordering of initialisation. <code>active_memory_manager</code> must be set before <code>new</code> is called anywhere. <code>VGA::EnterGraphicsMode</code> must come after any text-mode output you want to see. The ATA driver&#39;s <code>Activate</code> call (which calls <code>Identify</code>) must happen before <code>FAT32::Mount</code>. Getting these in the wrong order produces failures that look unrelated to the actual cause.</p>\n",
    "wordCount": 1150,
    "readingTime": 6,
    "assets": []
  },
  {
    "slug": "building my own os from scratch:  p1 & 2",
    "title": "Writing an x86 OS: Protected Mode, Interrupts, and Hardware Drivers",
    "description": "Starting the bare-metal x86 OS: GDT, IDT, PIC remapping, port I/O, PS/2 keyboard and mouse drivers, and PCI bus enumeration.",
    "date": "2026-01-15",
    "modified": "2026-01-15",
    "tags": [
      "OS",
      "x86",
      "systems",
      "assembly",
      "c++",
      "low-level"
    ],
    "people": [
      "author"
    ],
    "type": "article",
    "draft": false,
    "html": "<h1>Writing an x86 OS: Protected Mode, Interrupts, and Hardware Drivers</h1>\n<p>GRUB hands you a kernel in 32-bit protected mode with a stack pointer and a multiboot info struct. Everything from that point,   memory layout, interrupt handling, hardware communication,   is yours to define. There is no <code>printf</code>, no <code>malloc</code>, no OS beneath you. This post covers the first two phases of building that foundation from scratch.</p>\n<hr>\n<h2>The GDT: A Mandatory Formality</h2>\n<p>The Global Descriptor Table is required to be in place in protected mode, but for a flat-model kernel it does almost nothing useful. Base 0, limit 4GB, G=1 for both code and data,   the GDT translates every logical address straight through to its linear equivalent. Its real job here is satisfying the CPU&#39;s requirement that <code>CS</code>, <code>DS</code>, and friends contain valid selectors before you do anything else.</p>\n<p>The entry encoding is the part worth understanding. The 8-byte descriptor format has base and limit split across non-contiguous bit ranges,   a historical artefact from the 286. In practice you encode it like this:</p>\n<pre><code class=\"hljs language-cpp\">entry.limit_low   = limit &amp; <span class=\"hljs-number\">0xFFFF</span>;\nentry.granularity = (flags &amp; <span class=\"hljs-number\">0xF0</span>) | ((limit &gt;&gt; <span class=\"hljs-number\">16</span>) &amp; <span class=\"hljs-number\">0x0F</span>);\nentry.base_low    = base &amp; <span class=\"hljs-number\">0xFFFF</span>;\nentry.base_mid    = (base &gt;&gt; <span class=\"hljs-number\">16</span>) &amp; <span class=\"hljs-number\">0xFF</span>;\nentry.base_high   = (base &gt;&gt; <span class=\"hljs-number\">24</span>) &amp; <span class=\"hljs-number\">0xFF</span>;\n</code></pre><p>The access byte for a ring-0 code segment is <code>0x9A</code>: present (<code>P=1</code>), DPL 0, S=1 (code/data), type <code>0xA</code> (execute/read). For data: <code>0x92</code>.</p>\n<p>After <code>lgdt</code>, you need a far jump to reload <code>CS</code> with the new code selector. Until that happens, the CPU still caches the old descriptor. The jump is just:</p>\n<pre><code class=\"hljs language-asm\">jmp $0x08, $flush\nflush:\n    mov $0x10, %ax\n    mov %ax, %ds   ; and es, fs, gs, ss\n</code></pre><p><code>__attribute__((packed))</code> on both the descriptor struct and the GDTR struct is non-negotiable,   the compiler will pad them otherwise and the CPU will read garbage.</p>\n<hr>\n<h2>The IDT and PIC: Interrupt Infrastructure</h2>\n<p>The IDT maps 256 interrupt vectors to handler addresses via 8-byte gate descriptors. The structure is straightforward,   split the handler address into low and high 16-bit halves, set the selector to <code>0x0008</code> (kernel CS), <code>type_attr</code> to <code>0x8E</code> for a 32-bit interrupt gate, and you&#39;re done.</p>\n<p>The critical issue before installing the IDT is the PIC. By default the 8259A master maps IRQ 0–7 to vectors 8–15, directly colliding with Intel&#39;s reserved exception vectors. You must remap it before <code>sti</code>. The initialisation sequence,   ICW1 through ICW4,   must be sent in strict order with an I/O wait between writes:</p>\n<pre><code>0x11 → 0x20 (master cmd) | 0x11 → 0xA0 (slave cmd)   // ICW1: initialise\n0x20 → 0x21                                            // ICW2: master offset 32\n0x28 → 0xA1                                            // ICW2: slave offset 40\n0x04 → 0x21                                            // ICW3: slave on IRQ2\n0x02 → 0xA1                                            // ICW3: connected to master IRQ2\n0x01 → 0x21 | 0x01 → 0xA1                              // ICW4: 8086 mode\n</code></pre><p>The assembly stubs handle the ABI mismatch between the CPU&#39;s interrupt entry mechanism and C++ calling conventions. A GAS macro generates one stub per vector:</p>\n<pre><code class=\"hljs language-asm\">.macro ISR_NOERRCODE num\n.global isr_\\num\nisr_\\num:\n    pusha\n    push %ds; push %es; push %fs; push %gs; push %ss\n    mov %esp, %eax\n    push %eax\n    movb $\\num, %al\n    push %eax\n    call _ZN16InterruptManager15HandleInterruptEhj\n    add $8, %esp\n    mov %eax, %esp\n    pop %ss; pop %gs; pop %fs; pop %es; pop %ds\n    popa\n    iret\n.endm\n</code></pre><p>One thing to get right: the mangled name. <code>InterruptManager</code> is 16 characters, not 14,   the correct symbol is <code>_ZN16InterruptManager15HandleInterruptEhj</code>. Verify with <code>nm</code> on the compiled object before assuming the string is correct.</p>\n<p>EOI handling is straightforward,   <code>0x20</code> to <code>0x20</code> for master IRQs, and for slave IRQs (8–15) <code>0x20</code> to <code>0xA0</code> first, then <code>0x20</code> to <code>0x20</code>. Missing the EOI stalls that IRQ line permanently.</p>\n<hr>\n<h2>Port I/O: The Hardware Communication Layer</h2>\n<p>Every hardware device on a PC exposes its registers through the I/O address space, a separate 16-bit address domain from memory, accessed via <code>in</code>/<code>out</code>. You can&#39;t touch these from C++ without inline assembly:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-function\"><span class=\"hljs-type\">uint8_t</span> <span class=\"hljs-title\">Read</span><span class=\"hljs-params\">()</span> <span class=\"hljs-type\">const</span> </span>{\n    <span class=\"hljs-type\">uint8_t</span> result;\n    <span class=\"hljs-function\">__asm__ <span class=\"hljs-title\">volatile</span><span class=\"hljs-params\">(<span class=\"hljs-string\">&quot;inb %1, %0&quot;</span> : <span class=\"hljs-string\">&quot;=a&quot;</span>(result) : <span class=\"hljs-string\">&quot;Nd&quot;</span>(port))</span></span>;\n    <span class=\"hljs-keyword\">return</span> result;\n}\n<span class=\"hljs-function\"><span class=\"hljs-type\">void</span> <span class=\"hljs-title\">Write</span><span class=\"hljs-params\">(<span class=\"hljs-type\">uint8_t</span> value)</span> </span>{\n    <span class=\"hljs-function\">__asm__ <span class=\"hljs-title\">volatile</span><span class=\"hljs-params\">(<span class=\"hljs-string\">&quot;outb %0, %1&quot;</span> :: <span class=\"hljs-string\">&quot;a&quot;</span>(value), <span class=\"hljs-string\">&quot;Nd&quot;</span>(port))</span></span>;\n}\n</code></pre><p>The <code>&quot;Nd&quot;</code> constraint handles the encoding: immediate if the port fits in 8 bits, <code>DX</code> register otherwise. <code>volatile</code> prevents the compiler reordering or eliminating the instruction. A <code>Port8BitSlow</code> subclass adds a write to <code>0x80</code> (POST diagnostic port) after each write, providing a ~1µs delay for slow peripherals like the PIC.</p>\n<hr>\n<h2>PS/2 Keyboard</h2>\n<p>The 8042 microcontroller sits between the CPU and PS/2 devices. It exposes two ports: <code>0x60</code> (data) and <code>0x64</code> (status/command). The status register&#39;s OBF bit (bit 0) tells you data is available to read; IBF (bit 1) tells you the controller is busy,   you must wait for it to clear before writing.</p>\n<p>The keyboard generates Scancode Set 1 (the 8042 translates from Set 2 automatically on most hardware). Make codes are <code>&lt; 0x80</code>, break codes are <code>make | 0x80</code>. Modifier tracking is a simple set of bool flags:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-keyword\">if</span> (scancode == <span class=\"hljs-number\">0x2A</span> || scancode == <span class=\"hljs-number\">0x36</span>) { shift = <span class=\"hljs-literal\">true</span>;  <span class=\"hljs-keyword\">return</span> esp; }\n<span class=\"hljs-keyword\">if</span> (scancode == <span class=\"hljs-number\">0xAA</span> || scancode == <span class=\"hljs-number\">0xB6</span>) { shift = <span class=\"hljs-literal\">false</span>; <span class=\"hljs-keyword\">return</span> esp; }\n<span class=\"hljs-keyword\">if</span> (scancode == <span class=\"hljs-number\">0x3A</span>) { caps_lock = !caps_lock; <span class=\"hljs-keyword\">return</span> esp; }\n</code></pre><p>For printable characters, <code>shift ^ caps_lock</code> selects which of the two 88-entry lookup tables to use.</p>\n<hr>\n<h2>PS/2 Mouse</h2>\n<p>The mouse is on the 8042&#39;s second channel and is disabled by default. Enabling it requires three steps through the controller:</p>\n<ol>\n<li>Send <code>0xA8</code> to <code>0x64</code>,   enable the auxiliary device</li>\n<li>Read the 8042 command byte, set bit 1 (AUX IRQ enable), write it back</li>\n<li>Send <code>0xD4</code> to <code>0x64</code> (prefix: next byte goes to mouse), then <code>0xF4</code> to <code>0x60</code> (enable data reporting), then consume the <code>0xFA</code> ACK</li>\n</ol>\n<p>Each mouse movement or click fires IRQ12 once per byte,   three interrupts per 3-byte packet. A cycle counter tracks position in the packet:</p>\n<pre><code class=\"hljs language-cpp\">buffer[offset++] = data_port.<span class=\"hljs-built_in\">Read</span>();\n<span class=\"hljs-keyword\">if</span> (offset != <span class=\"hljs-number\">3</span>) <span class=\"hljs-keyword\">return</span> esp;\noffset = <span class=\"hljs-number\">0</span>;\n<span class=\"hljs-comment\">// process complete packet</span>\n</code></pre><p>The Y axis is inverted: PS/2 positive Y means upward movement, but screen Y increases downward, so <code>y -= dy</code>.</p>\n<p>The status register&#39;s MOBF bit (bit 5) distinguishes mouse data from keyboard data. If it&#39;s not set at interrupt time, bail out.</p>\n<hr>\n<h2>PCI Bus Enumeration</h2>\n<p>PCI configuration space is accessed through two I/O ports: <code>0xCF8</code> (address) and <code>0xCFC</code> (data). You write a 32-bit address to <code>0xCF8</code>,   bit 31 set as an enable, bus in bits 23:16, device in 15:11, function in 10:8, register offset in 7:2,   then read or write <code>0xCFC</code>.</p>\n<p>Enumeration is brute force: iterate bus 0–7, device 0–31, function 0–7. Read vendor ID at offset <code>0x00</code>. If it returns <code>0xFFFF</code>, nothing&#39;s there. The header type byte at <code>0x0E</code> bit 7 tells you whether the device is multi-function,   if it&#39;s not, skip the remaining functions.</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-type\">uint32_t</span> addr = (<span class=\"hljs-number\">1</span> &lt;&lt; <span class=\"hljs-number\">31</span>) | (bus &lt;&lt; <span class=\"hljs-number\">16</span>) | (device &lt;&lt; <span class=\"hljs-number\">11</span>)\n              | (function &lt;&lt; <span class=\"hljs-number\">8</span>) | (offset &amp; <span class=\"hljs-number\">0xFC</span>);\naddress_port.<span class=\"hljs-built_in\">Write</span>(addr);\n<span class=\"hljs-keyword\">return</span> data_port.<span class=\"hljs-built_in\">Read</span>() &gt;&gt; (<span class=\"hljs-number\">8</span> * (offset % <span class=\"hljs-number\">4</span>));\n</code></pre><p>The shift at the end handles sub-dword reads,   PCI config space is always read as full dwords, so byte and word reads at aligned offsets need the result shifted down.</p>\n<p>At this stage, enumeration just identifies what&#39;s present. The class code at <code>0x0B</code> combined with subclass at <code>0x0A</code> gives you the device category,   <code>0x01/0x01</code> for IDE storage, <code>0x02/0x00</code> for Ethernet, <code>0x03/0x00</code> for VGA.</p>\n<hr>\n<h2>Build Setup</h2>\n<p>The build is straightforward: <code>g++</code> with <code>-m32 -nostdlib -fno-exceptions -fno-rtti -fno-builtin</code>, <code>as --32</code>, <code>ld -melf_i386</code> against a custom linker script. The script places <code>.multiboot</code> first (the GRUB magic numbers), then <code>.text</code>, then <code>.data</code> (where the C++ constructor list lives between <code>start_ctors</code> and <code>end_ctors</code> symbols), then <code>.bss</code>.</p>\n<p>Constructor invocation is manual:</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-keyword\">extern</span> <span class=\"hljs-string\">&quot;C&quot;</span> <span class=\"hljs-function\"><span class=\"hljs-type\">void</span> <span class=\"hljs-title\">callConstructors</span><span class=\"hljs-params\">()</span> </span>{\n    <span class=\"hljs-keyword\">for</span> (constructor* i = &amp;start_ctors; i != &amp;end_ctors; ++i)\n        (*i)();\n}\n</code></pre><p>This is what fires before <code>kernelMain</code>,   without it, any C++ object with a non-trivial constructor at file scope would be uninitialised.</p>\n<p>Booted and tested in QEMU (<code>qemu-system-i386 -cdrom mykernel.iso</code>). The <code>-d int</code> flag pipes every interrupt to stderr, which is invaluable for tracking down triple faults during interrupt handler development.</p>\n",
    "wordCount": 996,
    "readingTime": 5,
    "assets": []
  },
  {
    "slug": "eireplan_building",
    "title": "Eireplan: Building for Clarity in Irish Planning",
    "description": "A personal note on why I’m building Eireplan, an experiment in traceability, planning knowledge, and making complex systems easier to navigate.",
    "date": "2025-02-09",
    "modified": "2025-02-09",
    "tags": [
      "eireplan",
      "planning",
      "startup",
      "engineering"
    ],
    "people": [
      "author"
    ],
    "type": "article",
    "draft": false,
    "html": "<h1>Eireplan</h1>\n<hr>\n<h2><img src=\"/blog/eireplan_building_eireplan_4579d276a745.webp\" alt=\"Eireplan\"></h2>\n<p>I’ve always been interested in systems that shape everyday life but rarely get talked about unless something goes wrong. Planning is one of those systems.</p>\n<p>It sits underneath housing, infrastructure, climate decisions, and the way our towns and cities evolve. It’s detailed, procedural, and often misunderstood. Most people only encounter it once or twice in their lives. Professionals live inside it every day.</p>\n<p>Eireplan started as a side project born out of curiosity: what would it look like if planning knowledge was easier to navigate, easier to trace, and easier to explain?</p>\n<p>Not simplified, just clearer.</p>\n<h2>Why planning feels harder than it should</h2>\n<p>If you’ve ever looked at a planning file, you’ll know the challenge isn’t a lack of information. It’s the opposite. There are policies layered on policies, national legislation intersecting with local plans, and long histories of decisions that quietly influence how new applications are assessed.</p>\n<p>Much of the work is repetitive. Checking completeness. Re-reading the same policy sections. Searching for similar past cases. Making sure nothing obvious has been missed.</p>\n<p>The system isn’t broken, but it is demanding. Even small errors can invalidate an application before it’s ever assessed, which adds time, cost, and frustration for everyone involved.</p>\n<p>That friction is what caught my attention.</p>\n<h2>What I’m trying to explore</h2>\n<p>Eireplan is my attempt to explore a different way of interacting with planning information.</p>\n<p>At its core, it’s an experiment in traceability.</p>\n<p>Instead of treating planning as a black box, where conclusions appear without clear lineage, the idea is to surface where information comes from, which documents matter, and how conclusions are formed. Not to replace professional judgement, but to support it.</p>\n<p>I’m interested in tools that help people think better, not just work faster.</p>\n<p>That means:</p>\n<ul>\n<li>being able to ask natural questions and see the policy basis behind the answer,</li>\n<li>understanding how similar cases were decided before,</li>\n<li>and keeping the logic visible, not hidden.</li>\n</ul>\n<h2>Building as a form of learning</h2>\n<p>This project isn’t driven by a single “big idea” moment. It’s grown incrementally, through reading, prototyping, talking to people in the system, and testing assumptions.</p>\n<p>Some weeks it’s technical. Other weeks it’s about understanding how planners actually work, or why certain processes exist the way they do. Often it’s both.</p>\n<p>I enjoy that tension. It’s where design, engineering, and policy collide.</p>\n<p>Eireplan is still evolving, and that’s intentional. I’m less interested in declaring it finished than in continuing to refine the questions it asks.</p>\n<h2>Why I’m sharing this</h2>\n<p>I wanted to write this down not as a statement of arrival, but as a marker of intent.</p>\n<p>I care about building tools that respect complex domains rather than flatten them. I care about clarity, accountability, and making invisible work more visible. And I enjoy the act of inventing things that sit quietly in the background and make difficult systems feel a little more navigable.</p>\n<p>Eireplan is one expression of that.</p>\n",
    "wordCount": 495,
    "readingTime": 3,
    "assets": [
      "/blog/eireplan_building_eireplan_4579d276a745.webp"
    ]
  }
];
